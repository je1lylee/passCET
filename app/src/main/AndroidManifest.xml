<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="top.linxixiangxin.passcet">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    \
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DengluActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Zhuce1Activity" />
        <activity android:name=".Zhuce2Activity" />
        <activity android:name=".YibeiActivity" />
        <activity android:name=".ShengciActivity" />
        <activity android:name=".ZXJActivity" />
        <activity android:name=".Study1Activity" />
        <activity android:name=".TimeActivity" />
        <activity android:name=".UserActivity"></activity>
    </application>

</manifest>